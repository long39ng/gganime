# an unexpected panel child count aborts

    Code
      name_layer_grobs(panel_gtable(2L), 3L)
    Condition
      Error in `name_layer_grobs()`:
      ! Cannot identify the layer grobs of a panel.
      x "panel" has 6 child grobs, expected 7.
      i This is a ggplot2 layout change; gganime needs updating.

