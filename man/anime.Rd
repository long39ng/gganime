% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anime.R
\name{anime}
\alias{anime}
\title{Render a gganimate plot as an animated-SVG widget}
\usage{
anime(
  plot,
  nframes = 100,
  fps = 10,
  duration = NULL,
  width = 640,
  height = 480,
  precision = 2,
  loop = TRUE,
  controls = TRUE,
  elementId = NULL,
  ...
)
}
\arguments{
\item{plot}{A \code{gganim} plot: a ggplot2 plot with a \verb{transition_*()} added.}

\item{nframes}{Number of frames to sample the animation at.}

\item{fps}{Frames per second, setting the playback speed.}

\item{duration}{Optional total duration in seconds; overrides \code{fps}.}

\item{width, height}{Widget size in pixels, defaulting to 640 by 480. The plot
is rendered at these dimensions, so their ratio sets the plot's aspect ratio.}

\item{precision}{Decimal places to round animated coordinates to.}

\item{loop}{Loop the animation: \code{TRUE}, \code{FALSE}, or a number of iterations.}

\item{controls}{Show a play/pause and scrub control bar.}

\item{elementId}{Optional htmlwidget element id.}

\item{...}{Unused; reserved for future arguments.}
}
\value{
An \code{htmlwidget} of class \code{gganime}.
}
\description{
Builds a ggplot2 plot written with gganimate syntax and renders it as a
self-contained, resolution-independent animated-SVG htmlwidget powered by
Anime.js, instead of gganimate's frame-by-frame output.
}
\examples{
library(ggplot2)
library(gganimate)

p <- ggplot(mtcars, aes(mpg, wt)) +
  geom_point(aes(colour = factor(cyl))) +
  transition_states(gear)

\donttest{
anime(p, nframes = 20)
}
}
